---
# Example inventory for the mahadzar81.adagios collection
# Copy this to your own inventory directory and adjust as needed.

all:
  children:
    monitoring:
      hosts:
        mon01.example.com:
          ansible_user: ec2-user          # RHEL/Rocky default; change to 'ubuntu' for Ubuntu
          ansible_become: true
          ansible_become_method: sudo
